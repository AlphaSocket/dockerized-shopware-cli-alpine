#!/bin/sh
#
# Run command
#
set -e

# Configuration
$(which docker-config)

echo "### Starting container service"
sh -c "/usr/sbin/crond -f -l $CONFIG_CRON_LOG_LEVEL"

# Container command is failed if exiting here
exit $?